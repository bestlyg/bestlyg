class Solution:
    def getDecimalValue(self, head: Optional[ListNode]) -> int:
        cur = 0
        while head:
            cur = cur << 1 | head.val
            head = head.next
        return cur