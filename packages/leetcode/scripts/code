class Solution:
    def rowAndMaximumOnes(self, mat: List[List[int]]) -> List[int]:
        res = 0
        resVal = 0
        for i in range(len(mat)):
            cnt = mat[i].count(1)
            if cnt > resVal:
                res = i
                resVal = cnt
        return [res, resVal]