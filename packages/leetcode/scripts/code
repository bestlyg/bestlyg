class Solution:
    def maximumTripletValue(self, nums: List[int]) -> int:
        return max(
            max((nums[i] - nums[j]) * nums[k] for k in range(len(nums)) for j in range(k) for i in range(j)),
            0
        )