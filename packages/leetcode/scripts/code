class Solution:
    def maxEvents(self, events: List[List[int]]) -> int:
        events.sort(reverse = True) 
        nmax = max(e[1] for e in events) + 1
        last = 0
        q = []
        res = 0
        for i in range(1, nmax):
            while q and q[0] < i: heappop(q)
            while events and events[-1][0] <= i:
                e = events.pop()
                heappush(q, e[1])
            if q:
                res += 1
                heappop(q)
        return res