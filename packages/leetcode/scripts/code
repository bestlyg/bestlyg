class Solution:
    def beautifulSubarrays(self, nums: List[int]) -> int:
        counter = Counter()
        counter[0] += 1
        cur = res = 0
        for num in nums:
            cur ^= num
            res += counter[cur]
            counter[cur] += 1
        return res