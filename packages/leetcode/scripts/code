class Solution:
    def isBalanced(self, num: str) -> bool:
        return sum(int(num[i]) for i in range(len(num)) if i % 2 == 0) == \
               sum(int(num[i]) for i in range(len(num)) if i % 2 != 0)