class Solution:
    def answerString(self, word: str, numFriends: int) -> str:
        if numFriends == 1: return word
        res = ''
        n = len(word)
        for i in range(n):
            res = max(res, word[i: n - numFriends + i + 1])
        return res