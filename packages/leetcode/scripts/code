class Solution:
    def isPowerOfThree(self, n: int) -> bool:
        i = 0
        while True:
            num = 3 ** i
            if num > n: return False
            if num == n: return True
            i += 1