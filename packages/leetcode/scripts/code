class Solution:
    def minOperations(self, nums: List[int], k: int) -> int:
        nums = sorted(Counter(nums).keys())
        idx = bisect_left(nums, k)
        if idx != 0: return -1
        if nums[idx] == k: idx += 1
        return len(nums) - idx