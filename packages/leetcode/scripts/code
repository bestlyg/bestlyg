class Solution:
    def findLucky(self, arr: List[int]) -> int:
        res = -1
        for key, cnt in Counter(arr).items():
            if key == cnt:
                res = max(res, cnt)
        return res