class Solution:
    def maximumDifference(self, nums: List[int]) -> int:
        nmin = nums[0]
        res = -1
        for num in nums:
            if num > nmin:
                res = max(res, num - nmin)
            nmin = min(nmin, num)
        return res