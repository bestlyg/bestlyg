class Solution:
    def minimumSum(self, n: int, k: int) -> int:
        used = set()
        res = 0
        cur = 1
        for _ in range(n):
            while k - cur in used: cur += 1
            res += cur
            used.add(cur)
            cur += 1
        return res