class Solution:
    def generate(self, numRows: int) -> List[List[int]]:
        res = [[1], [1, 1]]
        while len(res) < numRows:
            res.append([1] + [n1 + n2 for n1, n2 in pairwise(res[-1])] + [1])
        return res[: numRows]