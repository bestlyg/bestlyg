class Solution:
    def largestCombination(self, candidates: List[int]) -> int:
        res = 0
        for i in range(32):
            cnt = 0
            for v in candidates:
                if v >> i & 1:
                    cnt += 1
            res = max(res, cnt)
        return res