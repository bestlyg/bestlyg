class Solution:
    def doesValidArrayExist(self, derived: List[int]) -> bool:
        n = len(derived)
        def run(start: int) -> bool:
            arr = [start]
            for i in range(n):
                arr.append(derived[i] ^ arr[-1])
            return arr[0] == arr[-1]
        return run(0) or run(1)