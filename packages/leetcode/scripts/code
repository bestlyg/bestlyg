class Solution:
    def findMatrix(self, nums: List[int]) -> List[List[int]]:
        counter = Counter(nums)
        res = []
        for num, cnt in counter.items():
            for i in range(cnt):
                if len(res) < i + 1: res.append([])
                res[i].append(num)
        return res