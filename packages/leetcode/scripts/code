class Solution:
    def minimumOperations(self, nums: List[int]) -> int:
        for i in range(0, len(nums), 3):
            if len(set(nums[i:])) == len(nums[i:]):
                return i // 3
        return 1 + (len(nums) - 1) // 3