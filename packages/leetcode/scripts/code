class Solution:
    def countBalls(self, lowLimit: int, highLimit: int) -> int:
        res = Counter()
        for cur in range(lowLimit, highLimit + 1):
            res[sum(int(c) for c in str(cur))] += 1
        return max(res.values())