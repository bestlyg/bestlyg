class Solution:
    def divideString(self, s: str, k: int, fill: str) -> List[str]:
        s = s.ljust(len(s) + k - len(s) % k if len(s) % k else len(s), fill)
        return [s[i: i + k] for i in range(0, len(s), k)]