arr = [Counter(str(2 ** i)) for i in range(30)]

class Solution:
    def reorderedPowerOf2(self, n: int) -> bool:
        c1 = Counter(str(n))
        for c2 in arr:
            if c1 == c2:
                return True
        return False