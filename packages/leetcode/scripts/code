class Solution:
    def maxSubsequence(self, nums: List[int], k: int) -> List[int]:
        idxs = sorted([i for i in range(len(nums))], key = lambda i: nums[i], reverse = True)
        return [nums[i] for i in sorted(idxs[:k])]