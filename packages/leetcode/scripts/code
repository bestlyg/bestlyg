class Solution:
    def countBadPairs(self, nums: List[int]) -> int:
        res = 0
        counter = Counter()
        for i in range(len(nums)):
            res += i - counter[nums[i] - i]
            counter[nums[i] - i] += 1
        return res