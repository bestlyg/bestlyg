class Solution:
    def divisorSubstrings(self, num: int, k: int) -> int:
        nstr = str(num)
        res = 0
        for i in range(k, len(nstr) + 1):
            subs = nstr[i - k: i]
            if int(subs) and num % int(subs) == 0: res += 1
        return res