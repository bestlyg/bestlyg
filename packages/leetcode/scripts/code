class Solution:
    def countLargestGroup(self, n: int) -> int:
        counter = Counter()
        for num in range(1, n + 1):
            v = sum(int(c) for c in str(num))
            counter[v] += 1
        nmax = max(counter.values())
        return sum(nmax == v for v in counter.values())