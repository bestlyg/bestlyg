class Solution:
    def intersect(self, nums1: List[int], nums2: List[int]) -> List[int]:
        c1 = Counter(nums1)
        c2 = Counter(nums2)
        res = []
        for num, cnt in c1.items():
            res += [num] * min(c2[num], cnt)
        return res