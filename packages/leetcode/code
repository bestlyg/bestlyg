class Solution:
    def minChanges(self, n: int, k: int) -> int:
        res = 0
        for i in range(32):
            v1 = (n >> i) & 1
            v2 = (k >> i) & 1
            if v2 and not v1: return -1
            if v2 != v1: res += 1
        return res