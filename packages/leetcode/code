class Solution:
    def countWays(self, nums: List[int]) -> int:
        nums.sort()
        n = len(nums)
        res = 0
        if nums[0] != 0: res += 1
        res += sum(nums[i] < i + 1 < nums[i + 1] for i in range(n - 1))
        if nums[-1] < n: res += 1
        return res