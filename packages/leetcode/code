class Solution:
    @cache
    def twoEggDrop(self, n: int) -> int:
        if n == 1: return 1
        res = inf
        for v in range(1, n + 1):
            res = min(res, max(v - 1, self.twoEggDrop(n - v)) + 1)
        return res