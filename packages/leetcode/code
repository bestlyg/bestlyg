class Solution:
    def duplicateNumbersXOR(self, nums: List[int]) -> int:
        res = 0
        for num, cnt in Counter(nums).items():
            if cnt == 2:
                res ^= num
        return res