class Solution:
    def minOperations(self, nums: List[int]) -> int:
        n = len(nums)
        f = False
        cnt = 0
        for i in range(n):
            cur = nums[i]
            if f: cur ^= 1
            if cur == 0:
                cnt += 1
                f = not f
        return cnt