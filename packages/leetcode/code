class Solution:
    def validStrings(self, n: int) -> List[str]:
        if n == 1: return ['0', '1']
        res = []
        for v in self.validStrings(n - 1):
            if v[-1] == '1': res.append(v + '0')
            res.append(v + '1')
        return res