class Solution:
    def minimumBoxes(self, apple: List[int], capacity: List[int]) -> int:
        nsum = sum(apple)
        capacity.sort()
        res = 0
        while nsum > 0 and capacity:
            nsum -= capacity.pop()
            res += 1
        return res