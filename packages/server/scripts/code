class Solution:
    def countOperations(self, num1: int, num2: int, cnt = 0) -> int:
        if num1 == 0 or num2 == 0: return cnt
        if num1 >= num2: return self.countOperations(num1 - num2, num2, cnt + 1)
        return self.countOperations(num1, num2 - num1, cnt + 1)