class Solution:
    def totalMoney(self, n: int) -> int:
        prev = res = cur = 0
        for day in range(n):
            if day % 7 == 0:
                prev += 1
                cur = prev
            res += cur
            cur += 1
        return res