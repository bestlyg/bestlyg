class Solution:
    def maximumEnergy(self, energy: List[int], k: int) -> int:
        n = len(energy)
        @cache
        def run(idx: int) -> int:
            if idx >= n: return 0
            return energy[idx] + run(idx + k)
        return max(run(i) for i in range(n))