class Solution:
    def minTimeToVisitAllPoints(self, points: List[List[int]]) -> int:
        def d(p1: List[int], p2: List[int]) -> int:
            return max(abs(p1[0] - p2[0]), abs(p1[1] - p2[1]))
        return sum(d(p1, p2) for p1, p2 in pairwise(points))