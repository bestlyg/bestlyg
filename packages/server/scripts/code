class Solution:
    def minimumCost(self, nums: List[int]) -> int:
        return min(
            nums[i] + nums[j]
            for i in range(1, len(nums))
            for j in range(i + 1, len(nums))
        ) + nums[0]