class Solution:
    def minDeletionSize(self, strs: List[str]) -> int:
        return sum(
            any(strs[i1][j] > strs[i2][j] for i1, i2 in pairwise(range(len(strs))))
            for j in range(len(strs[0]))
        )