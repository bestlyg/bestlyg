class Solution:
    def getNoZeroIntegers(self, n: int) -> List[int]:
        for n1 in range(1, n):
            if str(n1).count('0') == 0 and str(n - n1).count('0') == 0:
                return [n1, n - n1]