class Solution:
    def constructTransformedArray(self, nums: List[int]) -> List[int]:
        n = len(nums)
        return [
            nums[(i + n + nums[i]) % n] if nums[i] != 0 else nums[i]
            for i in range(n)
        ]