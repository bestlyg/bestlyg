class Solution:
    def removeAnagrams(self, words: List[str]) -> List[str]:
        s = [words[0]]
        for word in words:
            if Counter(s[-1]) != Counter(word):
                s.append(word)
        return s