class Solution:
    def hasSameDigits(self, s: str) -> bool:
        return self.hasSameDigits(''.join(str((int(v1) + int(v2)) % 10) for v1, v2 in pairwise(s))) \
        if len(s) > 2 else s[0] == s[1]