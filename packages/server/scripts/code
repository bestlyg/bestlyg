class Solution:
    def numberOfSpecialChars(self, word: str) -> int:
        res = 0
        for i in range(26):
            ch1 = chr(i + ord('A'))
            ch2 = chr(i + ord('a'))
            if ch1 in word and ch2 in word:
                res += 1
        return res