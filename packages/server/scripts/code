class Solution:
    def canBeTypedWords(self, text: str, brokenLetters: str) -> int:
        brokenset = set(brokenLetters)
        return sum(
            all([c not in brokenset for c in s])
            for s in text.split(' ')
        )