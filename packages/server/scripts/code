class Solution:
    def minBitwiseArray(self, nums: List[int]) -> List[int]:
        def get(num: int) -> int:
            for ans in range(num):
                if ans | (ans + 1) == num:
                    return ans
            return -1
        return [get(num) for num in nums] 