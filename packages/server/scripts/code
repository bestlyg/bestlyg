class Solution:
    def numberOfBeams(self, bank: List[str]) -> int:
        return sum(
            l1.count('1') * l2.count('1')
            for l1, l2 in pairwise(l for l in bank if l.count('1') != 0)
        ) 